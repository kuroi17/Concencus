const port = Number(process.env.PORT || 3001);

console.log(`[backend] starter server placeholder on port ${port}`);
console.log("[backend] Socket.IO and Supabase integration is added in next commit.");
